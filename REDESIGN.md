# 重新设计：《入梦·牡丹亭》

> 如果让我从零开始，我会把这套产品完全重做。
> 旧版的核心问题是——**它在"教"，所以学生觉得在做题**。
> 新版的核心是——**它在"入"，所以学生觉得自己在做梦**。

---

## 一、立意：把"读"换成"入"

| 维度 | 旧版（教） | 新版（入） |
|---|---|---|
| 体验隐喻 | 一堂 AI 辅读课 | 一场昆曲梦 |
| 用户身份 | 做题的学生 | 入园的梦中人 |
| 文本角色 | 被分析的语料 | 梦中浮现的念白 |
| AI 角色 | 路径规划器 | 梦境的织者 |
| 互动 | 选词、点按钮、答问卷 | 注视、停留、让文字自己动 |
| 反馈 | 弹窗 + 高亮 | 涟漪 + 余韵 |
| 出口 | 「回到原文再读一遍」 | 自然醒（屏幕渐暗，下次重开） |

不教读者任何东西。让读者"碰巧"在梦里和杜丽娘走过同一段路。

---

## 二、体验结构：三折

不做「游园 / 游园 / 余韵」三段并列（这也是在做题）。
做「一段路，三种光」——**同一段《皂罗袍》原文，在三折里以不同形态出现**。

- **第一折【游园】**：文字如水面浮萍，颜色清淡，几乎不动
- **第二折【惊梦】**：文字如镜中倒影，开始跟随目光微微晃动
- **第三折【余韵】**：文字如晨雾散去，密度变低，留白变大

三折不是「章节」，是「光的浓度」。读者不知道自己正在过折。

---

## 三、交互范式：水流式注意

旧版最大败笔：**把选择做成了"打断"**。
读者在读，忽然要"点词"——这就把"读"和"互动"对立了。

新版核心原则：**没有"互动"，只有"看"**。

### 注视即选择

- 文字以「淡墨字」出现（rgba(39,35,31,.38)）
- 鼠标停留 / 触摸某字 ≥ 1.6s，该字从淡墨转为「胭脂」
- 同时从该字辐射出一道涟漪（css radial-gradient，3s 衰减）
- 涟漪扫过的其他字，若与该字有"语义亲缘"，也微微浮起

### 没有按钮

- 没有"选 A / 选 B"
- 没有"确认 / 继续"
- 没有"进入下一段"
- 唯一持续的提示在右下角：极淡的「随她去」（随时可隐去）

### 兜底机制

- 4 秒无任何停留 → 系统自动从最「中性」的字位触发一次涟漪
- 用户可以"什么都不做"地走完一遍

---

## 四、视觉语言：墨池

放弃"宣纸+印章"（太套路、像 PPT）。
采用**墨池**——一个会随时间微微加深的黑色水底。

### 三层

1. **底（墨池）**：背景从 #f7f3ea（清水）渐变到 #1c1a17（浓墨）
2. **中（文字）**：白色为主，浮于墨面，被涟漪扫过时短暂染胭脂
3. **面（涟漪 / 余韵）**：胭脂红 rgba(166,93,104,.45)，透明度可调

### 三折色温

- 游园：青绿（rgba(126,156,134,.22)）
- 惊梦：胭脂（rgba(166,93,104,.22)）
- 余韵：月白（rgba(214,210,196,.22)）

色温不是切换，是混合：游园里就有惊梦的影子，余韵里还能看到游园。

### 不再有

- 朱红印章
- 边框
- 弹窗
- 任何按钮的"按下"反馈

---

## 五、声音设计：水磨腔的呼吸

旧版的根问题是"配乐"思路——把音乐当背景。
新版是"腔"思路——声音就是文字本身。

### 三层声部

| 层 | 内容 | 音量 | 触发 |
|---|---|---|---|
| 底层 | 单音持续（箫/弦） | ≤0.10 | 始终 |
| 中层 | 装饰音（琵琶轮指、古琴泛音） | ≤0.25 | 涟漪扩散时 |
| 顶层 | 关键音（莺声、远钟） | ≤0.40 | 仅剧情必要位置 |

### 强约束

- 没有任何一次性"音效"（无"叮"、"咚"、"Duang"）
- 所有音 attack ≥ 80ms，decay 自然指数，无截断
- 关键音出现时，先降低底层音量 6dB（让位），结束后回升
- 底层音每 90-120s 换一次根音（五度内），永远听不出"在换"

### 不用现成音频文件

- 全部 Web Audio API 实时合成
- 频率锁定昆曲主调（建议 D 调）
- 装饰音引用原文句子的音高轮廓（OCR 后做音高映射）

---

## 六、AI 定位：梦境的织者

旧版 AI = 路径规划器。给三个候选，挑一个。
这暴露了"做题"本质。

新版 AI = **余韵召回器**。
- 不生成任何原文外的内容
- 不解释、不分析、不评判
- 只做一件事：用户停在哪一句，就从原文中"召回"一句与之有"意象亲缘"的句子，让它以「余韵」形式浮现

### 工作流（不是 prompt，是数据流）

```
用户注视某字 (t ≥ 1.6s)
  → 前端采集: 字、句、上下文 50 字
  → POST /api/whisper
    {
      anchor: { line_id, char, ts },
      context: "...前后 50 字...",
      worldState: { scene, attention_depth, time_in_scene }
    }
  → DeepSeek（极简 prompt）:
    "从以下原文中挑出与该句有'意象亲缘'的 1 句，
     不要解释，只回 line_id 与 reason（一词）"
  → 前端:
    1. 校验 line_id 是否在原文里（防幻觉）
    2. 校验 reason 是否在白名单
    3. 在锚字旁渲染一个 1.4rem 的「余」字
    4. 用户点「余」字 → 该余韵句在原处"涨"起来
    5. 涨起后 4s 回落
  → 全程无弹窗、无候选展示、无"为什么"
```

### 关键约束

- DeepSeek 永远是"召回"，不是"生成"
- 白名单 reason 词：缘、影、对、续、答、起、落、转、归
- 召回失败时，前端不报错，只是没有「余」字出现

---

## 七、技术架构

```
src/
  dream/
    state.ts            # 折态 (scene/attention_depth/time_in_scene)
    attention.ts        # 注视检测 (pointer + raf 节流)
    ripple.ts           # 涟漪渲染 (canvas + css 双轨)
    word-drift.ts       # 文字微动 (transform + filter)
  text/
    verses.ts           # 原文（按 line_id 索引）
    affinities.ts       # 预计算的"意象亲缘"图（可选，省 DeepSeek）
  audio/
    sheng-mian.ts       # 底层持续音（箫 + 弦 双声部）
    hua-ying.ts         # 装饰音（按音高轮廓合成）
    yuan-yin.ts         # 关键音（莺声 / 远钟）
    mixer.ts            # 三层混音 + ducking
  vision/
    mo-chi.ts           # 墨池背景（shader 或多层径向渐变）
    yan-zhi.ts          # 胭脂染色（覆盖层）
  agent/
    whisper.ts          # 余韵召回（唯一调用 DeepSeek 的地方）
  api/
    whisper.ts          # /api/whisper 路由
  store.ts
  App.tsx
  performance.css       # 全部样式
```

### 关键变化

- **去掉** ReadingBias、StageAdaptive、StagingCandidates、PerformanceScore 一整套"打分系统"
- **去掉** Plan / Echo / 教学闭环的命名
- **唯一保留的 AI 调用**：`/api/whisper`（召回 + 校验）

---

## 八、保留 vs 放弃

### 保留

- DeepSeek 参与核心逻辑
- 原文 / 今译 / 导览三层共存
- 顶部 4 控件（全屏 / 今译 / 声音 / 动效）
- 默认开启声音
- 持久"下一步"提示（极淡）
- 真人访谈环节不变

### 放弃

- 选项卡式互动
- 弹窗式 AI 反馈
- 问卷、测试、版本号、体验编号
- 任何 dev 痕迹
- "回到原文"按钮
- 加载问卷
- 朱红印章 / 边框 / 选项卡
- 「A 版 / B 版」分支逻辑
- Performance、Score、Plan、Echo 等术语

---

## 九、产品名与首屏

- 产品名：《入梦·牡丹亭》
- 首屏只有一行字（淡墨色，居中）：
  > 你看见一个园子。你走了进去。
- 右下角极淡的：「随她去」
- 无标题、无 logo、无说明
- 5 秒无动作 → 文字开始微微浮动，背景开始加深

---

## 十、为什么要这么改

旧版的问题是**它太想"教"了**。
它给学生一个文本，给一个 AI，让学生"配合 AI 完成任务"。

但《牡丹亭》不是用来"学"的，是用来"碰"的。
一个高中生读不懂「则为你如花美眷」没关系，
只要他/她在读到这一句时，**屏幕微微染了一点胭脂**，
**远处有一声箫**，
**他自己也不知道为什么，停留了 1.6 秒**，
**然后「余」字出现了**。

这就够了。

AI 不需要解释什么叫"如花美眷"。
读者停留本身，就是理解。

---

## 附：与本次访谈研究的关系

访谈研究（`research/`）的方法、提纲、记录表 **完全保留**。
它研究的是"人怎么读"，这是产品本身应该回应的对象。

但产品本身应该**藏起研究的痕迹**——
读者不应该看到任何"研究版本"、"测试编号"、"体验者"。
就像戏台上的演员不会告诉观众"我在做田野调查"。
